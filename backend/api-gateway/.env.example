# =============================================================================
# Aapda Setu API Gateway - Environment Configuration
# =============================================================================
# Copy to .env: cp .env.example .env
# =============================================================================

# Server
PORT=5000
NODE_ENV=development

# =============================================================================
# DATABASE - MongoDB (REQUIRED)
# =============================================================================
# IMPORTANT: Use lowercase 'aapdasetu' for consistency
MONGODB_URI=mongodb://127.0.0.1:27017/aapdasetu

# =============================================================================
# CACHE - Redis (Optional)
# =============================================================================
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# =============================================================================
# JWT AUTHENTICATION (REQUIRED)
# =============================================================================
# ⚠️ CHANGE IN PRODUCTION!
JWT_SECRET=g+hcFkUqpJYGW76BwhkncMro/59Q+Pfh78J6o3cQYpU=
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=7d

# =============================================================================
# OLLAMA AI (REQUIRED for chatbot)
# =============================================================================
# Use 127.0.0.1 instead of localhost for reliability
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=aapda-assistant

# =============================================================================
# ML SERVICES (Optional)
# =============================================================================
ML_CPU_URL=http://127.0.0.1:8000
ML_GPU_URL=http://127.0.0.1:8001
HMAC_SECRET=gM2832ZNeGTndS4srJHV5pcxdqgb+Xm7/KigP9QjnQQ=

# =============================================================================
# OPTIONAL SERVICES (Leave as dummy for MVP)
# =============================================================================

# Firebase (not required for MVP)
FIREBASE_PROJECT_ID=dummy
FIREBASE_PRIVATE_KEY="dummy"
FIREBASE_CLIENT_EMAIL=dummy@dummy

# AWS S3 (not required for MVP)
AWS_REGION=local
AWS_ACCESS_KEY_ID=local
AWS_SECRET_ACCESS_KEY=local
S3_BUCKET_NAME=local

# Notifications (not required for MVP)
FCM_SERVER_KEY=local
TWILIO_ACCOUNT_SID=local
TWILIO_AUTH_TOKEN=local
TWILIO_PHONE_NUMBER=+1234567890

# =============================================================================
# CORS & RATE LIMITING
# =============================================================================
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000
